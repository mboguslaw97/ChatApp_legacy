/* eslint-disable */
// @ts-nocheck

import { StyleSheet } from 'react-native';

import { Colors } from '../../global/colors';

export type Styles = {};

const createStyleSheet = (colors: Colors): Styles => StyleSheet.create({});

export default createStyleSheet;
